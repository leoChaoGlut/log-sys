spring:
  cloud:
    config:
      server:
        git:
          uri: file:///${user.home}/config-repo
          search-paths: '{application}'
eureka:
  client:
    service-url:
      defaultZone: http://192.168.75.129:10100/eureka/
  instance:
    prefer-ip-address: true
    ip-address: 192.168.75.129
    instance-id: ${random.value}
server:
  port: 10200