spring:
  application:
    name: collector-test
  cloud:
    config:
      discovery:
        enabled: true
        service-id: log-service-center-config-center
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:10100/eureka/
