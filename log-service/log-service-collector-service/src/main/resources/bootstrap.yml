spring:
  application:
    name: collector-service-localhost
  cloud:
    config:
      discovery:
        enabled: true
        service-id: log-service-center-config-center
eureka:
  client:
    serviceUrl:
      defaultZone: http://log.yunyichina.cn:10100/eureka/
