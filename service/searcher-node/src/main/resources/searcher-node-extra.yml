constants:
  upload:
    logSuffix: .log
    indexSuffix: .index
    bakSuffix: .bak
    zipIndex: .zip
    logRootDir: D:${file.separator}uploadLogRoot
    indexRootDir: D:${file.separator}upload${file.separator}uploadIndexRoot
  index:
    rootDir: D:${file.separator}indexRoot
server:
  port: 10500
