spring:
  application:
    name: collector-node
  profiles:
    active: dev,extra
  cloud:
    config:
      enabled: true
      discovery:
        serviceId: config-center
        enabled: true
eureka:
  client:
    healthcheck:
      enale: true
    serviceUrl:
      defaultZone: http://192.168.75.1:10100/eureka/
logging:
  config: classpath:logback.xml
